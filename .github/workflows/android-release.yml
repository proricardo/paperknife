name: Build & Sign Android APK

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Source
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: 'npm'

      - name: Install Dependencies
        run: npm ci

      - name: Build Web Assets (Standard)
        run: npm run build

      - name: Generate Android Assets
        run: npx -y @capacitor/assets generate --android

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: '21'

      - name: Sync & Build (Standard)
        run: |
          npx cap sync android
          cd android && chmod +x gradlew && ./gradlew assembleRelease
          mv app/build/outputs/apk/release/app-release-unsigned.apk app/build/outputs/apk/release/standard-unsigned.apk

      - name: Build Web Assets (F-Droid)
        run: |
          rm -rf public/tesseract/*.wasm
          export VITE_DISABLE_OCR=true
          npm run build

      - name: Sync & Build (F-Droid)
        run: |
          npx cap sync android
          cd android && ./gradlew assembleRelease
          mv app/build/outputs/apk/release/app-release-unsigned.apk app/build/outputs/apk/release/fdroid-unsigned.apk

      - name: Sign APKs
        uses: ilharp/sign-android-release@v2
        with:
          releaseDir: android/app/build/outputs/apk/release
          signingKey: ${{ secrets.ANDROID_SIGNING_KEY }}
          keyAlias: ${{ secrets.ANDROID_ALIAS }}
          keyStorePassword: ${{ secrets.ANDROID_KEYSTORE_PASSWORD }}
          keyPassword: ${{ secrets.ANDROID_KEY_PASSWORD }}
          buildToolsVersion: "34.0.0"

      - name: Prepare Final Files
        run: |
          VERSION=$(node -p "require('./package.json').version")
          cp android/app/build/outputs/apk/release/standard-unsigned-signed.apk "paperknife-v${VERSION}.apk"
          cp android/app/build/outputs/apk/release/fdroid-unsigned-signed.apk "paperknife-fdroid-v${VERSION}.apk"
          echo "APP_VERSION=${VERSION}" >> $GITHUB_ENV

      - name: Upload APKs
        uses: actions/upload-artifact@v4
        with:
          name: PaperKnife-v${{ env.APP_VERSION }}
          path: |
            paperknife-v${{ env.APP_VERSION }}.apk
            paperknife-fdroid-v${{ env.APP_VERSION }}.apk
          if-no-files-found: error
          compression-level: 0